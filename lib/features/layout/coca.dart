import 'package:flutter/material.dart';

class CocaColaa extends StatelessWidget {
  const CocaColaa({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: double.infinity,
      height: double.infinity,
      color: Colors.blueGrey,
    );
  }
}
